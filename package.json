{
  "dependencies": {
    "three": "^0.166.1",
    "troika-three-text": "^0.49.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.3.3"
  },
  "scripts": {
     "test": "echo \"Error: no test specified\" && exit 1",
     "deploy": "gh-pages -d dist"
  }
}
